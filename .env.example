# When configured, the frontend reads and writes clients/orders through the
# Pizza Box Agent API service from backend/. Leave empty to use local demo storage.
VITE_API_BASE_URL=

# Example after backend/ is deployed as its own Railway service:
# VITE_API_BASE_URL=https://<pizza-box-backend-service>.up.railway.app
