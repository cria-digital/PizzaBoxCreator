# When configured, the frontend reads and writes clients/orders through the
# Pizza Box Agent API backed by Postgres. Leave empty to use local demo storage.
VITE_API_BASE_URL=

# Example after the backend is deployed as a separate Railway service:
# VITE_API_BASE_URL=https://pizza-box-agent-api-production.up.railway.app
